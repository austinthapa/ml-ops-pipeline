apiVersion: v1
kind: Service
metadata:
  name: heart-attack-pred-service
spec:
  type: LoadBalancer
  selector:
    app: heart-attack-pred
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080